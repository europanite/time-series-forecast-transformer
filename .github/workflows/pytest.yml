name: Pytest
on:
  push:
  pull_request:
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true
jobs:
  tests:
    name: pytest - ${{ matrix.os }} - py${{ matrix.python-version }}
    runs-on: ${{ matrix.os }}
    permissions:
      contents: read
      actions: write
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        python-version: [ "3.10", "3.11", "3.12", "3.13", "3.14" ]
    env:
      PYTHONUTF8: "1"
      PYTHONPATH: ${{ github.workspace }}/service/src
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: ${{ matrix.python-version }}
          cache: "pip"
          cache-dependency-path: service/requirements.test.txt

      - name: Install deps (Unix)
        if: matrix.os != 'windows-latest'
        shell: bash
        run: |
          set -eux
          python -m pip install -U pip
          python -m pip install -r service/requirements.test.txt
          python -m pip install -e service --no-deps
      - name: Install deps (Windows)
        if: matrix.os == 'windows-latest'
        shell: pwsh
        run: |
          $ErrorActionPreference = "Stop"
          python -m pip install -U pip
          python -m pip install -r service/requirements.test.txt
          python -m pip install -e service --no-deps
      - name: Show versions
        shell: bash
        run: |
          python --version
          pip --version
          python -m pytest --version
          python -c "import sys,platform,os;print(platform.platform());print(sys.version);print('PYTHONPATH=',os.environ.get('PYTHONPATH'))"
      - name: Run tests with coverage
        shell: bash
        run: |
          python -m coverage run -m pytest service/tests -q
          python -m coverage xml -o coverage.xml
      - name: Upload coverage.xml
        uses: actions/upload-artifact@v7
        with:
          name: coverage-${{ matrix.os }}-py${{ matrix.python-version }}
          path: coverage.xml
          if-no-files-found: error

