# Chronos-2 currently requires Transformers 4.x. Do not pin Transformers 5.x here.
# TimesFM 2.5 is loaded through the official google-research/timesfm package,
# because its Transformers port requires Transformers 5.x and conflicts with Chronos-2.
# Keep torch pinned. The Dockerfile selects the CUDA/CPU wheel index explicitly.
huggingface-hub==1.32.0
torch==2.14.0
transformers==5.17.0
timesfm[torch] @ git+https://github.com/google-research/timesfm.git
-e .